const TopBar = () => {
  return (
        <div style={{backgroundColor: 'var(--foreground)', color: 'var(--background)'}} className="text-center p-4 mt-auto border-t">
      <div className={`container mx-auto`}>
        <h1 className="text-4xl font-bold">Asistencia y Metricas</h1>
        <p>Todo el control sobre tu Pyme en un solo lugar</p>
      </div>
    </div>
);
};

export default TopBar;